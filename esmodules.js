import add, { subtract, multiply, divide } from "./math/esindex.js";

console.log(add(5, 5));
console.log(subtract(10, 5));
console.log(multiply(5, 5));
console.log(divide(10, 2));

// para trabajar como esmodules agregar type modules a package.json
